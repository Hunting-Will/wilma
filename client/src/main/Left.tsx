import { Box } from '@mui/material';
import { Game } from '../../../types';

import { styled } from '@mui/system';


export function Left({ game }: { game: Game }) {

    return (
        <Box flexDirection="column" display="flex">
            Something
        </Box>
    );
}
